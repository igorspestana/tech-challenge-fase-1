"""End-to-end ML pipeline script (for Docker and CLI execution)."""

from __future__ import annotations


def main() -> None:
    """Run the full pipeline: load data, preprocess, train, evaluate, save artifacts."""
    raise NotImplementedError(
        "Implement the pipeline steps and wire notebooks logic here for reproducible runs."
    )


if __name__ == "__main__":
    main()
